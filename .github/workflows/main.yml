# SPDX-License-Identifier: MIT
# Copyright (C) 2026 VIKINGYFY

#云编译公用核心
name: MAIN-CORE

on:
  workflow_call:
    inputs:
      WRT_CONFIG:
        required: true
        type: string
      WRT_THEME:
        required: true
        type: string
      WRT_NAME:
        required: true
        type: string
      WRT_SSID:
        required: true
        type: string
      WRT_WORD:
        required: true
        type: string
      WRT_IP:
        required: true
        type: string
      WRT_PW:
        required: true
        type: string
      WRT_REPO:
        required: true
        type: string
      WRT_BRANCH:
        required: true
        type: string
      WRT_SOURCE:
        required: true
        type: string
      WRT_PACKAGE:
        required: false
        type: string
      WRT_TEST:
        required: false
        type: string
      WRTPackageManager:
        description: '包管理器类型：ipk 或 apk'
        required: false
        type: string
        default: 'ipk'
      UPLOAD_TYPE:
        description: '固件上传方式：release 或 artifact'
        required: false
        type: string
        default: 'release'

env:
  WRT_CONFIG: ${{inputs.WRT_CONFIG}}
  WRT_THEME: ${{inputs.WRT_THEME}}
  WRT_NAME: ${{inputs.WRT_NAME}}
  WRT_SSID: ${{inputs.WRT_SSID}}
  WRT_WORD: ${{inputs.WRT_WORD}}
  WRT_IP: ${{inputs.WRT_IP}}
  WRT_PW: ${{inputs.WRT_PW}}
  WRT_REPO: ${{inputs.WRT_REPO}}
  WRT_BRANCH: ${{inputs.WRT_BRANCH}}
  WRT_SOURCE: ${{inputs.WRT_SOURCE}}
  WRT_PACKAGE: ${{inputs.WRT_PACKAGE}}
  WRT_TEST: ${{inputs.WRT_TEST}}
  WRTPackageManager: ${{inputs.WRTPackageManager}}
  UPLOAD_TYPE: ${{inputs.UPLOAD_TYPE}}
  DINGDING_TOKEN: ${{ secrets.DINGDING_TOKEN }}

jobs:
  core:
    name: ${{inputs.WRT_SOURCE}}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Projects
        uses: actions/checkout@main

      - name: Initialization Environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt -yqq purge firefox
          sudo -E apt -yqq update
          sudo -E apt -yqq full-upgrade
          sudo -E apt -yqq autoremove --purge
          sudo -E apt -yqq autoclean
          sudo -E apt -yqq clean
          sudo -E apt -yqq install dos2unix libfuse-dev
          sudo bash -c 'bash <(curl -sL https://build-scripts.immortalwrt.org/init_build_environment.sh)'
          sudo -E systemctl daemon-reload
          sudo -E timedatectl set-timezone "Asia/Shanghai"

          sudo mkdir -p /mnt/build_wrt
          sudo chown $USER:$USER /mnt/build_wrt
          sudo ln -s /mnt/build_wrt $GITHUB_WORKSPACE/wrt

      - name: Initialization Values
        run: |
          echo "WRT_DATE=$(TZ=UTC-8 date +"%y.%m.%d-%H.%M.%S")" >> $GITHUB_ENV
          echo "WRT_MARK=${GITHUB_REPOSITORY%%/*}" >> $GITHUB_ENV
          echo "WRT_INFO=${WRT_SOURCE%%/*}" >> $GITHUB_ENV
          echo "WRT_TARGET=$(grep -m 1 -oP '^CONFIG_TARGET_\K[\w]+(?=\=y)' ./Config/$WRT_CONFIG.txt)" >> $GITHUB_ENV
          echo "WRT_WIFI=wifi-yes" >> $GITHUB_ENV
          echo "WRT_KVER=none" >> $GITHUB_ENV
          echo "WRT_LIST=none" >> $GITHUB_ENV

      - name: Clone Code
        run: |
          git clone --depth=1 --single-branch --branch $WRT_BRANCH $WRT_REPO ./wrt/

          cd ./wrt/ && echo "WRT_HASH=$(git log -1 --pretty=format:'%h')" >> $GITHUB_ENV

          # GitHub Action 移除国内下载源
          PROJECT_MIRRORS_FILE="./scripts/projectsmirrors.json"
          if [ -f "$PROJECT_MIRRORS_FILE" ]; then
            sed -i '/.cn\//d; /tencent/d; /aliyun/d' "$PROJECT_MIRRORS_FILE"
          fi

      - name: Check Scripts
        run: |
          find ./ -maxdepth 3 -type f -iregex ".*\(txt\|sh\)$" -exec dos2unix {} \; -exec chmod +x {} \;

      - name: Check Caches
        id: check-cache
        if: env.WRT_TEST != 'true'
        uses: actions/cache@main
        with:
          key: ${{env.WRT_CONFIG}}-${{env.WRT_INFO}}-${{env.WRT_HASH}}
          restore-keys: ${{env.WRT_CONFIG}}-${{env.WRT_INFO}}
          path: |
            ./wrt/.ccache
            ./wrt/staging_dir/host*
            ./wrt/staging_dir/tool*

      - name: Update Caches
        if: env.WRT_TEST != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -d "./wrt/staging_dir" ]; then
            find "./wrt/staging_dir" -type d -name "stamp" -not -path "*target*" | while read -r DIR; do
              find "$DIR" -type f -exec touch {} +
            done

            mkdir -p ./wrt/tmp && echo "1" > ./wrt/tmp/.build

            echo "toolchain skiped done!"
          else
            echo "caches missed!"
          fi

          if ${{steps.check-cache.outputs.cache-hit != 'true'}}; then
            CACHE_LIST=$(gh cache list --key "$WRT_CONFIG-$WRT_INFO" | cut -f 1)
            for CACHE_KEY in $CACHE_LIST; do
              gh cache delete $CACHE_KEY
            done

            echo "caches cleanup done!"
          fi

      - name: Update Feeds
        run: |
          cd ./wrt/

          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Custom Packages
        run: |
          cd ./wrt/package/

          $GITHUB_WORKSPACE/Scripts/Packages.sh
          $GITHUB_WORKSPACE/Scripts/Handles.sh

      - name: Custom Settings
        run: |
          cd ./wrt/

          if [[ "${WRT_CONFIG,,}" == *"test"* ]]; then
            cat $GITHUB_WORKSPACE/Config/$WRT_CONFIG.txt >> .config
          else
            cat $GITHUB_WORKSPACE/Config/$WRT_CONFIG.txt $GITHUB_WORKSPACE/Config/GENERAL.txt >> .config
          fi

          $GITHUB_WORKSPACE/Scripts/Settings.sh

          # 保存用户原始配置（make defconfig 之前）
          cp -f ./.config ./my_config.txt

          make defconfig -j$(nproc)

          # 生成 seed.config（记录用户自定义配置与默认配置的差异）
          if [ -f ./scripts/diffconfig.sh ]; then
            bash ./scripts/diffconfig.sh > ./seed.config
          fi

          make clean -j$(nproc)

      - name: Download Packages
        if: env.WRT_TEST != 'true'
        run: |
          cd ./wrt/

          make download -j$(nproc)

      - name: Generate Start Notification
        if: always()
        working-directory: ./wrt
        run: |
          chmod +x $GITHUB_WORKSPACE/Scripts/ci_create_start_notification.sh
          bash $GITHUB_WORKSPACE/Scripts/ci_create_start_notification.sh

      - name: Send Start DingTalk Notification
        if: always() && env.DINGDING_TOKEN != ''
        env:
          DINGDING_MESSAGE: |
            ${{ env.start_notify_content }}
        run: |
          payload=$(python3 -c 'import json, os; print(json.dumps({"msgtype":"text","text":{"content": os.environ["DINGDING_MESSAGE"]}}, ensure_ascii=False))')
          curl --fail --silent --show-error \
            -H 'Content-Type: application/json' \
            -d "$payload" \
            "https://oapi.dingtalk.com/robot/send?access_token=${DINGDING_TOKEN}"

      - name: Compile Firmware
        id: compile
        continue-on-error: true
        if: env.WRT_TEST != 'true'
        run: |
          cd ./wrt/

          make -j$(nproc) || make -j1 V=s

      - name: Finalize Status
        if: always()
        run: |
          echo "COMPILE_STATUS=${{ steps.compile.outcome || 'skipped' }}" >> $GITHUB_ENV
          echo "END_TIME=$(TZ=UTC-8 date +"%y.%m.%d-%H.%M.%S")" >> $GITHUB_ENV

      - name: Machine Information
        run: |
          cd ./wrt/

          echo "======================="
          lscpu | grep -E "name|Core|Thread"
          echo "======================="
          df -h
          echo "======================="
          du -h --max-depth=1
          echo "======================="

      - name: Package Firmware
        if: steps.compile.outcome == 'success' || env.WRT_TEST == 'true'
        working-directory: ./wrt
        run: |
          chmod +x $GITHUB_WORKSPACE/Scripts/ci_organize_outputs.sh
          bash $GITHUB_WORKSPACE/Scripts/ci_organize_outputs.sh

      - name: Release Firmware
        if: env.UPLOAD_TYPE == 'release' && env.COMPILE_STATUS == 'success'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          release_tag="${WRT_CONFIG}-${WRT_INFO}-${WRT_BRANCH}-${WRT_DATE}"
          release_files=($(find ./wrt/upload/ -type f))

          if [ ${#release_files[@]} -eq 0 ]; then
            echo "错误：未找到可上传的固件文件"
            exit 1
          fi

          release_body="这是个平台固件包，内含多个设备！
          请注意选择你需要的设备固件！
          不要问，刷就完事了！

          全系带开源硬件加速，别问了！

          源码：${WRT_REPO}
          分支：${WRT_BRANCH}
          提交：${WRT_HASH}

          配置：${WRT_CONFIG}
          平台：${WRT_TARGET}

          登录地址：${WRT_IP}
          登录密码：${WRT_PW}

          WIFI名称：${WRT_SSID}
          WIFI密码：${WRT_WORD}

          内核版本：${WRT_KVER}
          插件列表：${WRT_LIST}"

          if gh release view "${release_tag}" >/dev/null 2>&1; then
            echo "更新已有 Release：${release_tag}"
            gh release edit "${release_tag}" --title "${release_tag}" --notes "${release_body}"
            gh release upload "${release_tag}" "${release_files[@]}" --clobber
          else
            echo "创建新的 Release：${release_tag}"
            gh release create "${release_tag}" "${release_files[@]}" --title "${release_tag}" --notes "${release_body}"
          fi

      - name: Upload Firmware To Artifact
        if: env.UPLOAD_TYPE == 'artifact' && env.COMPILE_STATUS == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: ${{env.WRT_CONFIG}}-${{env.WRT_INFO}}-${{env.WRT_BRANCH}}-${{env.WRT_DATE}}
          path: ./wrt/upload

      - name: Generate Notification
        if: always()
        working-directory: ./wrt
        run: |
          chmod +x $GITHUB_WORKSPACE/Scripts/ci_create_notifications.sh
          bash $GITHUB_WORKSPACE/Scripts/ci_create_notifications.sh

      - name: Send DingTalk Notification
        if: always() && env.DINGDING_TOKEN != ''
        env:
          DINGDING_MESSAGE: |
            ${{ env.notify_content }}
        run: |
          payload=$(python3 -c 'import json, os; print(json.dumps({"msgtype":"text","text":{"content": os.environ["DINGDING_MESSAGE"]}}, ensure_ascii=False))')
          curl --fail --silent --show-error \
            -H 'Content-Type: application/json' \
            -d "$payload" \
            "https://oapi.dingtalk.com/robot/send?access_token=${DINGDING_TOKEN}"
